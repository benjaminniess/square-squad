'use strict'

class Bonus {
  constructor(params) {}

  getData() {
    return {
      x: this.x,
      y: this.y,
      width: this.width,
      height: this.height,
    }
  }
}

module.exports = Bonus
